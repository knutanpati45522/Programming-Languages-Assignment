def calculate_sum(arr):
    total = o   # Syntax/Name error: 'o' is not defined
    for num in arr:
        total += num
    return total

numbers = [1, 2, 3, 4, 5]
result = calculate_sum(numbers)
print(result)
